# AGENTS.md

Greenfield repo. `README.md` (ES): vehicle journal app ("diario de tus vehiculos").
`.gitignore` is Node-style; no code, manifests, CI, or OpenCode config yet.

- No build/test/lint commands exist. Do not invent any; add them here when the stack lands.
- Keep docs/UX copy in Spanish unless asked otherwise (README is Spanish).
- Use Conventional Commits for commit messages (e.g. `docs: add AGENTS.md ...`).
- Update this file when the stack, entrypoints, and commands are decided; keep it to verified facts only.
